#include <stdio.h>
#include "myheader.h"
int i = 0;

int main()
{
      increment();
      increment();
      increment();
      increment();
      increment();
      increment();
      printf("i = %i\n",i);
      
      return 0;
}